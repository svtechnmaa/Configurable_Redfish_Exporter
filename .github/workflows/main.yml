name: Build and Push PR Image

# Fires on every PR comment, including on issues — the job-level `if`
# below narrows this to an exact `/build` comment on a pull request.
on:
  issue_comment:
    types: [created]

# One in-flight build per PR: a second /build cancels the previous one
# instead of racing it for the same `pr-<PR_NUMBER>` tag.
concurrency:
  group: pr-build-${{ github.event.issue.number }}
  cancel-in-progress: true

env:
  IMAGE_NAME: 'redfish-exporter'

jobs:
  build:
    name: Build and push redfish-exporter
    runs-on: self-hosted
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout PR head commit
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.gate.outputs.head_sha }}
          persist-credentials: false

      - name: Read package version
        id: version
        run: |
          VERSION=$(python3 setup.py --version)
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: false
          load: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}:pr-${{ github.event.issue.number }}
          build-args: |
            appVersion=${{ steps.version.outputs.version }}
            maintainer=Freezing

      # Login only happens after a successful build, and only this step
      # and the next can push — build failure above stops the job here.
      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: '${{ github.actor }}'
          password: '${{ secrets.GITHUB_TOKEN }}'

      - name: Push image
        run: docker push "ghcr.io/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}:pr-${{ github.event.issue.number }}"
